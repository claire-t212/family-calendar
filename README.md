# Семейный Календарь — PWA

Современное веб-приложение для совместного планирования с поддержкой работы офлайн, устанавливаемое на iPhone и Android.

![Calendar Preview](./preview.png)

## ✨ Возможности

- 📅 **Три вида календаря**: месяц, неделя, день
- 👥 **Совместный доступ**: приглашайте семью и друзей
- ⚡ **Realtime синхронизация**: изменения видны мгновенно
- 🔔 **Напоминания**: push-уведомления о событиях
- 🎨 **17 цветов**: для визуального разделения событий
- 🌙 **Тёмная тема**: автоматическая или ручная
- 📱 **PWA**: установка на домашний экран
- 📶 **Offline**: работает без интернета

## 🛠 Технологии

- **Frontend**: React 18 + TypeScript + Vite
- **Стили**: Tailwind CSS
- **Backend**: Supabase (PostgreSQL)
- **Realtime**: Supabase Realtime
- **Auth**: Supabase Auth (Email + Magic Link)
- **State**: Zustand
- **PWA**: Vite PWA Plugin + Workbox

## 🚀 Быстрый старт

### 1. Клонируйте репозиторий

```bash
git clone https://github.com/your-username/collaboration-calendar-pwa.git
cd collaboration-calendar-pwa
```

### 2. Установите зависимости

```bash
npm install
```

### 3. Настройте Supabase

1. Создайте проект на [supabase.com](https://supabase.com)
2. В SQL Editor выполните скрипт из `supabase/schema.sql`
3. Включите Email аутентификацию в Authentication → Providers
4. Скопируйте URL и anon key из Settings → API

### 4. Настройте переменные окружения

```bash
cp .env.example .env
```

Заполните `.env`:
```
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
```

### 5. Запустите приложение

```bash
npm run dev
```

Откройте [http://localhost:5173](http://localhost:5173)

## 📱 Установка на iPhone

1. Откройте приложение в Safari
2. Нажмите кнопку "Поделиться" (квадрат со стрелкой)
3. Выберите "На экран «Домой»"
4. Нажмите "Добавить"

## 🌐 Деплой

### Vercel (рекомендуется)

1. Подключите репозиторий к [Vercel](https://vercel.com)
2. Добавьте переменные окружения
3. Деплой произойдёт автоматически

```bash
npm i -g vercel
vercel
```

### Netlify

```bash
npm run build
# Загрузите папку dist на Netlify
```

### Docker

```dockerfile
FROM node:20-alpine as build
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM nginx:alpine
COPY --from=build /app/dist /usr/share/nginx/html
EXPOSE 80
```

## 📁 Структура проекта

```
src/
├── components/
│   ├── calendar/       # Компоненты календаря
│   │   ├── Header.tsx
│   │   ├── Sidebar.tsx
│   │   ├── MonthView.tsx
│   │   ├── WeekView.tsx
│   │   └── DayView.tsx
│   ├── modals/         # Модальные окна
│   │   ├── EventModal.tsx
│   │   ├── CalendarModal.tsx
│   │   ├── InviteModal.tsx
│   │   └── SettingsModal.tsx
│   └── ui/             # UI компоненты
│       ├── Button.tsx
│       ├── Input.tsx
│       ├── Modal.tsx
│       └── ...
├── lib/
│   ├── supabase.ts     # Supabase клиент
│   ├── dateUtils.ts    # Работа с датами
│   ├── notifications.ts # Push-уведомления
│   └── constants.ts    # Константы
├── pages/
│   ├── AuthPage.tsx
│   ├── CalendarPage.tsx
│   └── InvitePage.tsx
├── stores/
│   ├── authStore.ts    # Авторизация
│   ├── calendarStore.ts # Календари
│   ├── eventStore.ts   # События
│   └── uiStore.ts      # UI состояние
├── types/
│   └── index.ts        # TypeScript типы
├── App.tsx
├── main.tsx
└── index.css
```

## 🔧 Скрипты

```bash
npm run dev      # Запуск dev сервера
npm run build    # Сборка для продакшена
npm run preview  # Превью продакшен сборки
npm run lint     # Проверка кода
```

## 🔐 Безопасность

- Row Level Security (RLS) в PostgreSQL
- Все запросы проверяются на сервере
- Токены приглашений с ограниченным сроком
- Раздельные роли: владелец, редактор, наблюдатель

## 📈 Будущие улучшения (v2)

- [ ] Повторяющиеся события
- [ ] Теги и категории
- [ ] Поиск по событиям
- [ ] Экспорт в iCal
- [ ] Интеграция с Google Calendar
- [ ] Загрузка файлов к событиям
- [ ] Комментарии к событиям
- [ ] Уведомления в Telegram

## 📄 Лицензия

MIT

---

Создано с ❤️ для совместного планирования
